# Assignment and block test
{ x=y=z=1, 1+x-y*z };

# If-else test
def NOT(x)
    if x then 0
    else 1
;

NOT(0);
NOT(1);

# For-loop test
def SUM(y)
{
    s = 0,
    for i = 0, i < y, i = i + 1, s = s + i
};

SUM(10);
